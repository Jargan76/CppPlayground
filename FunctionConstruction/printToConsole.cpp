#include "stdafx.h"
#include <iostream>

void printToConsole(int first, int op, int second, int result) {

	std::cout << "Here is the results you asked for " << first << " (still working on) " << second << " = " << result << std::endl;
}